// this script can fix various issues

// one issue when using `react-native run-android` - the run immediately fails with no error message. to solve this, we must chmod the gradlew file properly. see https://github.com/facebook/react-native/issues/8868
// chmod 755 android/gradlew
